# dependencies
node_modules/

# next.js
.next/
out/
build/
dist/
next-env.d.ts
*.tsbuildinfo

# env
.env*.local
.vercel

# os / editor
.DS_Store
*.log

# remotion film project (source is tracked; build artifacts are not)
my-video/node_modules/
my-video/out/
my-video/.remotion/

# dentai-landing is a git submodule (its own repo); its build artifacts
# are ignored by its own .gitignore.
